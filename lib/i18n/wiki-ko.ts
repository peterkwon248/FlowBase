// FlowBase — Wiki 시드 페이지 한국어본
//
// 왜 사전(ko.ts)이 아니라 별도 모듈인가:
// 위키 본문은 수백~수천 자 마크다운이라 사전 키로 쓰면 공백 하나 차이에도 매칭이
// 깨진다. 대신 **페이지 id로 매핑**하고, 렌더 시 "본문이 시드 원문 그대로인가"를
// 확인해서 한국어본을 쓸지 정한다 (lib/i18n/wiki.ts).
// → 사용자가 편집한 페이지는 절대 덮어쓰지 않는다.

export const wikiKo: Record<string, { title: string; category: string; body: string }> = {
  "wiki-library-intro": {
    title: "라이브러리 가이드",
    category: "개념",
    body: `# 라이브러리란?

라이브러리는 워크스페이스의 **재사용 가능한 정의 저장소**입니다. 셀 옵션, 컬럼 정의, 테이블 템플릿, 자동 채움 로직이 한곳에 모여 있고, 어디서든 가져다 쓸 수 있습니다.

스프레드시트와 결정적으로 다른 점: 같은 드롭다운을 5개 테이블에서 쓸 때, 라이브러리에서는 **한 번만 정의**합니다. 옵션 하나를 추가하면 5개가 즉시 갱신됩니다.

## 자산 4종

### 옵션 리스트
공유되는 옵션 묶음. 예: \`@Model\` (M-PEN-III, POGOPIN, DWHM-1 …), \`@Handling\`, \`@Business unit\`.

여러 컬럼이 같은 옵션 리스트를 공유할 수 있습니다. \`Order model\` 컬럼과 \`Return model\` 컬럼이 모두 \`@Model\`을 참조하면, 옵션 하나를 추가할 때 두 컬럼이 동시에 갱신됩니다.

### 필드
포장된 컬럼 정의. 이름 + 타입 + 옵션 + 기본값 + 검증 규칙이 한 묶음입니다.

예: \`@ReceivedAt\` = { date, 필수, 기본값: 오늘, 검증: 미래 날짜 불가 }

새 테이블에 컬럼을 추가할 때 \`@ReceivedAt\`을 가져오면 모든 설정이 자동으로 적용됩니다.

### 템플릿
여러 필드 + 추천 뷰 설정의 묶음. CS 팀의 \`@CS Case\` 템플릿에는 필드 6개 + 보드 뷰 추천 + 상태 그룹핑이 들어 있습니다.

새 테이블을 만들 때 템플릿을 고르면 5초 만에 완성된 구조가 나옵니다.

### 함수
재사용 가능한 스마트 함수. (이전 명칭: 규칙)

- \`MATCH_FROM_DROPDOWN\` — 텍스트에서 옵션 자동 추출
- \`AI_CLASSIFY\` — Claude가 의미를 분석해 분류
- \`EXTRACT_REGEX\` — 정규식 추출

스프레드시트 함수와 달리 이들은 **데이터베이스를 압니다**. \`MATCH_FROM_DROPDOWN(productName, @Model)\`은 라이브러리의 Model 옵션 리스트를 직접 참조합니다.

## 테이블과의 관계

| 작업 | 테이블에서 | 라이브러리에서 |
|---|---|---|
| 컬럼 추가 | "+" 버튼 → 필드 선택 | 필드 자체를 정의·수정 |
| 옵션 추가 | 셀에 직접 입력 | @optionList에 추가 — 전체 반영 |
| 새 테이블 | 스키마 → 템플릿 선택 | 템플릿 자체를 정의·관리 |

## Promote — 양방향

라이브러리에 모든 걸 미리 정의해둘 필요는 없습니다. 테이블에서 자유롭게 드롭다운을 만들다가 "이거 다른 데서도 쓰겠는데" 싶으면, 컬럼 헤더 메뉴에서 **Promote to Library**로 끌어올리면 됩니다.`,
  },

  "wiki-cs-runbook": {
    title: "CS 케이스 처리 절차",
    category: "런북",
    body: `# CS 케이스 처리 절차

## 신규 케이스 등록
1. 인입 채널 확인 (이메일 / 카카오톡 / 네이버 톡톡)
2. Tasks 테이블 → "+ 새 행" → 자동 입력된 모델명 확인
3. 자동 분류된 처리 유형 확인 (반짝임 표시)
4. 필요하면 수동으로 조정

## 처리 유형 선택
- **단순 변심**: 7일 이내, 미사용
- **오주문 교환**: 고객이 다른 모델을 주문
- **불량 교환 / 환불**: 제품 불량
- **A/S**: 보증 기간 내 수리
- **오배송 교환 / 환불**: 당사 배송 실수
- **택배 파손 교환 / 환불**: 배송 중 파손

## 상태 흐름
\`수거 접수\` → \`검수중\` → \`대기\` → \`완료\`

보드 뷰에서 드래그하거나 셀을 직접 편집해 상태를 옮깁니다.`,
  },

  "wiki-keyboard-shortcuts": {
    title: "키보드 단축키",
    category: "참조",
    body: `# 키보드 단축키

## 패널 토글
- \`⌘B\` — AI 패널
- \`⌘⇧A\` — 액티비티 바
- \`⌘⇧F\` — 사이드바
- \`⌘I\` — 디테일 바

## 편집
- \`⌘Z\` — 실행 취소
- \`⌘⇧Z\` — 다시 실행
- \`Delete\` / \`Backspace\` — 선택한 행 삭제

## 검색
- \`⌘K\` — 빠른 검색`,
  },

  "wiki-onboarding": {
    title: "신규 입사자 온보딩",
    category: "온보딩",
    body: `# 신규 입사자 온보딩 (초안)

## 1일차
- 워크스페이스 둘러보기
- 라이브러리 자산 4종 소개
- 첫 케이스 직접 처리해보기

## 2-3일차
- 스키마에서 테이블 관계 이해하기
- 자동화 흐름 익히기

(이 문서는 작성 중입니다. 검증되지 않았습니다.)`,
  },

  "wiki-glossary": {
    title: "용어집",
    category: "참조",
    body: `# 용어집

**라이브러리** — 워크스페이스의 재사용 가능한 정의 저장소.
**옵션 리스트** — 드롭다운/태그에 쓰는 옵션 묶음.
**필드** — 포장된 컬럼 정의 (타입 + 옵션 + 검증 등).
**템플릿** — 필드 묶음 + 추천 뷰.
**함수** — 셀 값을 자동으로 채우는 재사용 로직.
**스키마** — 워크스페이스의 테이블 구조와 관계.
**자동화** — 행/이벤트로 발동되는 워크플로 (테이블 간).
**Promote** — 테이블에서 만든 정의를 라이브러리로 끌어올리는 것.
**Cascade** — 라이브러리 자산 수정이 모든 사용처에 자동 전파되는 것.
**스마트 채움** — 새 컬럼에 함수를 연결해 자동으로 값을 채우는 것.`,
  },

  "wiki-team": {
    title: "팀 디렉토리",
    category: "팀",
    body: `# 팀 디렉토리

## CS 팀
- **호민지** — CS 총괄 · 케이스 우선순위 결정
- **한승호** — CS 담당 · 단순 변심 / 교환
- **박서연** — CS 담당 · 불량 / A/S

## 프로덕트
- **peter** — PM · 워크스페이스 운영
- **Daniel Park** — 엔지니어링 리드 · 스마트 채움 / 함수

## 보고 라인
호민지 → peter (매주 월요일 1:1)`,
  },
}
